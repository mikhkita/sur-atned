$(document).ready(function(){
	var menu_it = 7;
	fot (0; menu_it; 1) {
		var btn_id = $('p_services_menu li').attr('menu_btn_id');
		var offset = $('#menu_btn_id').offset();
	}
  

  // $( "#result" ).text( this.tagName +
  //   " coords ( " + offset.left + ", " + offset.top + " )" );
});


